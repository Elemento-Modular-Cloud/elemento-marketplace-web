version: "3.8"

services:
  mailserver:
    image: ghcr.io/docker-mailserver/docker-mailserver:latest
    # Provide the FQDN of your mail server here (Your DNS MX record should point to this value)
    hostname: mail.web.elemento.cloud
    env_file: mailserver.env
    # More information about the mail-server ports:
    # https://docker-mailserver.github.io/docker-mailserver/latest/config/security/understanding-the-ports/
    # To avoid conflicts with yaml base-60 float, DO NOT remove the quotation marks.
    ports:
      - "25:25"    # SMTP  (explicit TLS => STARTTLS, Authentication is DISABLED => use port 465/587 instead)
      - "143:143"  # IMAP4 (explicit TLS => STARTTLS)
      - "465:465"  # ESMTP (implicit TLS)
      - "587:587"  # ESMTP (explicit TLS => STARTTLS)
      - "993:993"  # IMAP4 (implicit TLS)
    volumes:
      - "/home/admin/dev/elemento-marketplace-web/mailserver/dms/mail-data/:/var/mail/"
      - "/home/admin/dev/elemento-marketplace-web/mailserver/dms/mail-state/:/var/mail-state/"
      - "/home/admin/dev/elemento-marketplace-web/mailserver/dms/mail-logs/:/var/log/mail/"
      - "/home/admin/dev/elemento-marketplace-web/mailserver/dms/config/:/tmp/docker-mailserver/"
      - /etc/localtime:/etc/localtime:ro
    restart: always
    stop_grace_period: 1m
    # Uncomment if using `ENABLE_FAIL2BAN=1`:
    # cap_add:
    #   - NET_ADMIN
    healthcheck:
      test: "ss --listening --tcp | grep -P 'LISTEN.+:smtp' || exit 1"
      timeout: 3s
      retries: 0