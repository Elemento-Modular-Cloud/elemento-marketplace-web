# traefik.yml

api:
  insecure: true

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false

#certificatesResolvers: # need to be defined properly
#  elementoResolver:
#    acme:
#      caServer: https://acme-staging-v02.api.letsencrypt.org/directory
#      storage: acme.json
